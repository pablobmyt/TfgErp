<Window x:Class="TfgErp.SplashScreen"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TfgErp"
        mc:Ignorable="d"
        Title="SplashScreen" Height="450" Width="800" WindowStyle="None" WindowState="Maximized">
    <Grid>
        <Ellipse Width="100" Height="100" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Ellipse.Style>
                <Style TargetType="Ellipse">
                    <Setter Property="Fill">
                        <Setter.Value>
                            <VisualBrush>
                                <VisualBrush.Visual>
                                    <ProgressBar IsIndeterminate="True" Style="{StaticResource RotatingProgressBarStyle}" />
                                </VisualBrush.Visual>
                            </VisualBrush>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Ellipse.Style>
        </Ellipse>
        <TextBlock Text="Iniciando sistema" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,30"/>
    </Grid>
</Window>
